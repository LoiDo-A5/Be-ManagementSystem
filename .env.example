# Server
PORT=3000

# PostgreSQL (Sequelize)
PG_HOST=localhost
PG_PORT=5432
PG_USER=postgres
PG_PASSWORD=your_password
PG_DB=team_management

# Sequelize options
# If true, will run ALTER TABLE to match models on startup (dev only)
SEQ_ALTER=true
# Log SQL statements
SEQ_LOG=false

# Auth
JWT_SECRET=super_secret_key_change_me
JWT_EXPIRES_IN=7d
